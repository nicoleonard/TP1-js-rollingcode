const numero = parseInt(prompt("ingrese un numero para verificar su divisibilidad en 2"));

if(numero%2==0){
    document.write("<p>El numero es divisible en 2</p>");
}else{
    document.write("<p>El numero no es divisible en 2</p>");
}